#include "common.h"

vector<int> solve(const vector<Node*>& nodes)
{
  // This is a sample solution.
  // Erase the code and put your solution here.
  return {0,1,3,2,4,5};
  vector<int> ret;
  for (int i = 0; i < nodes.size(); i++)
    ret.push_back(i);
  return ret;
}
